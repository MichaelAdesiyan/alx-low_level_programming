#include "main.h"
/**
 * add - function that adds two integers and returns the result
 * @a: added integer to b
 * @b: added integer to a
 * Return: void.
 */
int add(int a, int b)
{
	return (a + b);
}
